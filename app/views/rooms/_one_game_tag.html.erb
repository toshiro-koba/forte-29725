<div class="index">
  <div class="index-tag">
    <% unless question.game_tags[0] == nil %>
      <%= link_to tag_search_game_tag_path(question.game_tags[0].id), class: 'game_tag__registration' do %>
        <%= question.game_tags[0].game_title %>
      <% end %>
    <% end %>
  </div>
  <div class="index-decoration"></div>
</div>